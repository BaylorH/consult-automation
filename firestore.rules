rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Proposals collection - only authenticated fiftyflowers.com users
    match /proposals/{proposalId} {
      allow read, write: if request.auth != null
        && request.auth.token.email.matches('.*@fiftyflowers[.]com$');
    }
  }
}
